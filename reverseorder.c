#include <stdio.h>

int main() {
    int num,i;

    
    scanf("%d", &num);

    while (n!=0)
     {
        int number % 10;
        reverse = reverse * 10 + digit;
        number = number / 10;
    }

    printf("The number in reverse order is: %d\n", reverse);

    return 0;
}
