//wap to check the given number is armstrong or not using while loop
/*

#include<stdio.h>
#include<math.h>
int main()
{
    int num,digit,original,sum=0,count=0;
    scanf("%d",&num);
    original=num;
    while(num!=0)//1534,153,15,1
    {
        original/=10;//0 remove last no
        count++;//count=0+1+1+1+1
    }
    original=num;
    while(num!=0)
    {
        digit=num%10;//153%10=3 , 15%10=5  take last no..
        sum+=pow(digit,count);//3*3-->sum=0+27 
        //5^3=125
        //1^3=1
        num/=10;//153/10--> 15/10=1/10=0.00
    }
    if  (sum==num)
    {u
        printf("it is an armstorng number");
    }
    else
    {
        printf("it is not an armstrong number");

    }
    return 0;
    }
*/

//wap to check the given number is prime or not

#include<stdio.h>
void main()
{
    int 
}